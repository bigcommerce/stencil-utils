import BaseHooks from './base';

export default class ProductHooks extends BaseHooks {

    /**
     * @Constructor
     * @param {object} options
     */
    constructor(options) {
        this.options = options || {};
    }
}
