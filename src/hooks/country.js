import BaseHooks from './base';

export default class CountryHooks extends BaseHooks {

    /**
     * @Constructor
     * @param {object} options
     */
    constructor(options) {
        this.options = options || {};
    }
}
