name: Build

on:
  pull_request:
    branches: [ master, main ]
  push:
    branches: [ master, main ]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node: [14.x]

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Use Node.js ${{ matrix.node }}
        uses: actions/setup-node@v2-beta
        with:
          node-version: ${{ matrix.node }}

      # for private libraries. Note: only if PR is created in same repo
      - run: echo "//registry.npmjs.org/:_authToken=${{ NPM_TOKEN_READ_ONLY.NPM_TOKEN }}" >> ~/.npmrc
      - run: npm whoami
    
      - name: Install Dependencies
        run: npm i
      - name: Lint the code
        run: npm run lint

      - name: Run test with coverage
        run: npm run test-with-coverage
